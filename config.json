{
    "expected_formats": [
        {
            "name": "Transaction ID",
            "format": "^[A-Z]{3}-[0-9]{4}-[A-Z]{2}$"
        },
        {
            "name": "Transaction Amount",
            "format": "^\\d+(\\.\\d{1,2})?$"
        },
        {
            "name": "Transaction Date",
            "format": "^\\d{4}-\\d{2}-\\d{2}$"
        }
    ],
    "forbidden_inputs": [
        "DROP TABLE",
        "DELETE",
        "INSERT INTO",
        "UPDATE"
    ]
}